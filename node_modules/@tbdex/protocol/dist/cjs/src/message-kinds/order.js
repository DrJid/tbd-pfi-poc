var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { Message } from '../message.js';
/**
 * Message sent by Alice to the PFI to accept a Quote.
 * @beta
 */
var Order = /** @class */ (function (_super) {
    __extends(Order, _super);
    function Order() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        /** a set of valid Message kinds that can come after an order */
        _this.validNext = new Set(['orderstatus']);
        return _this;
    }
    /**
     * Creates an order with the given options
     * @param opts - options to create an order
     */
    Order.create = function (opts) {
        var metadata = __assign(__assign({}, opts.metadata), { kind: 'order', id: Message.generateId('order'), createdAt: new Date().toISOString() });
        var message = { metadata: metadata, data: {} };
        Message.validateData('order', message.data);
        return new Order(message);
    };
    return Order;
}(Message));
export { Order };
